version: '2'
services:
  hexo:
    build: ./app
    ports:
     - "80:3000"
    depends_on:
     - database-hexology
    environment:
     - DATABASE_HOST=database-hexology
  database-hexology:
    image: /Docker-DB