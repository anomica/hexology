version: '2'
services:
  hexo:
    image: gloverac/hexo:latest
    ports:
     - "3000:3000"
    depends_on:
     - database-hexology
    environment:
     - DATABASE_HOST=database-hexology
  database-hexology:
    image: gloverac/hexodb:latest